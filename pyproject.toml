[project]
name = "out-in-the-sun-site"
version = "0.1.0"
description = "Promotional website for the novel Out in the Sun"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1",
    "pyyaml>=6.0",
    "markdown>=3.6",
    "pillow>=11.3",
]

[dependency-groups]
dev = ["playwright>=1.47"]
